# Зависимости и сборка
node_modules/
/.next/
/out/
/build
dist/
.vite/
next-env.d.ts
*.tsbuildinfo

# Окружение и БД (SQLite — локальный файл, в git не храним)
.env
.env*.local
/prisma/*.db
/prisma/*.db-journal

# Большой видеофайл видеолекции (~114 МБ) превышает лимит GitHub в 100 МБ.
# Загружается на сервер отдельно через scp (см. DEPLOY.md), либо подключите Git LFS.
public/assets/video/cikly-lecture.mp4

# Системные / редакторские
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
_*.png

# Исходник фото в HEIC (не нужен в продакшене)
public/assets/img/author-source.heic
